defaults:
- main
- _self_

img_dir: data/interim_lung_seg
# Unet, Unet++, DeepLabV3, DeepLabV3+, FPN, PAN, MAnet
# Linknet and PSPNet work on torch == 1.8.1 and earlier
model_names: [DeepLabV3, FPN, MAnet]
save_dir: data/interim_fused
